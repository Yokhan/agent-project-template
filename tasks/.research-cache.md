# Research Cache

> Incremental research findings that persist across compaction and sessions.
> Agents: UPDATE entries, don't recreate from scratch. Add new sections, update existing.
> Pre-compact hook saves context here. Session-start restores from here.

## How to Use

### Writing (agents):
- After completing research-first protocol, append findings here under a dated section
- If a section for the same topic exists → UPDATE it (merge new findings), don't duplicate
- Keep entries concise: file paths, key findings, decisions made
- Delete stale entries when the related work is completed and committed

### Reading (agents):
- Check this file BEFORE running fresh research — the answer may already be here
- After compaction: this file IS your research context. Read it first.
- If an entry looks stale (references deleted files, old branches) → delete it

## Format

```
## [YYYY-MM-DD] Topic: [brief description]
**Files**: [paths read]
**Findings**: [key discoveries]
**Decisions**: [what was decided and why]
**Status**: active | completed | stale
```

---

## Entries

## [2026-05-19] Topic: Codex fan-out and Spec Kit adaptation
**Files**: `docs/CODEX_FANOUT_PATTERNS.md`, `docs/CODEX_SUBAGENTS_AUDIT.md`, `.agents/skills/codex-subagent-orchestration/SKILL.md`, `AGENTS.md`, `docs/AGENT_PIPELINES.md`, `scripts/test-codex-subagents-live.sh`, `scripts/validate-codex-agents.js`, `setup.sh`, `setup.bat`, `scripts/sync-template.sh`, `scripts/test-template.sh`, `scripts/validate-template.sh`, `../AgentOS/CLAUDE.md`, `../AgentOS/src-tauri/src/commands/ops.rs`, `../AgentOS/src-tauri/src/commands/chat_parse.rs`
**Findings**: GitHub Spec Kit's useful portable pattern is artifact-first work (`spec.md` -> `plan.md` -> `tasks.md` -> implementation), dependency-aware tasks with `[P]` parallel markers, and constitution/gate checks. This should be adapted without forcing `.specify/` into projects that already have litkit, Kiro, AgentOS, or other agent-flow systems. AgentOS consumes this template through stable setup/sync entrypoints and has its own Strategy/Tactic/Plan/Todo/Gate orchestration, so template changes must stay additive.
**Decisions**: Add `docs/CODEX_FANOUT_PATTERNS.md` as the routing matrix and prompt library; update Codex subagent docs and skill to discover existing artifacts before fan-out; keep Zed manual checklist out; add an explicit quota-consuming live test script that is opt-in; strengthen `validate-codex-agents.js` around Spark model, read-only defaults, implementer scope, and `max_depth = 1`.
**Status**: active

## [2026-05-19] Topic: Codex skills parity audit
**Files**: `CLAUDE.md`, `AGENTS.md`, `.codex/config.toml`, `.codex/hooks.json`, `.agents/skills/*/SKILL.md`, `.claude/skills/*/SKILL.md`, `.claude/pipelines/*`, `.claude/rules/router.md`, `scripts/sync-agents.sh`, `setup.sh`, `setup.bat`, `scripts/sync-template.sh`, `scripts/test-template.sh`, `scripts/validate-template.sh`, `scripts/check-drift.sh`, `scripts/validate-codex-skills.js`, `docs/CODEX_SKILLS_AUDIT.md`, `docs/AGENT_PIPELINES.md`, `docs/OPENAI_MODEL_GUIDANCE.md`, `docs/PRODUCT_BOUNDARY.md`, `docs/SAFE_DEFAULTS.md`, `docs/SUPPORTED_ENVIRONMENTS.md`, `$CODEX_HOME/skills/.system/skill-creator/SKILL.md`
**Findings**: Official Codex docs use `.agents/skills` for repository-scoped skills, not `.codex/skills`. Claude has 30 skills, 3 pipeline definitions, and 10 agent definitions plus `PROTOCOL.md`; direct copying is wrong because Claude skills lack Codex metadata and several are model/subagent-oriented. OpenAI model docs checked on 2026-05-19 recommend `gpt-5.5` as the starting point for complex reasoning/coding/API work, with GPT-5.4 mini/nano for latency/cost, Responses API for agentic workflows, and `reasoning.effort: "medium"` as the starting effort. Project `.codex/config.toml` must not hardcode model, effort, approval, or sandbox defaults.
**Decisions**: Implement a Codex-native skill layer under `.agents/skills` with 35 template-owned skills plus project-owned `.agents/skills/project-*` preservation. Add `docs/AGENT_PIPELINES.md` as the agent-neutral pipeline source, `docs/OPENAI_MODEL_GUIDANCE.md` as the official-docs snapshot, and `scripts/validate-codex-skills.js` as the Node-based validator. Extend setup, sync, smoke tests, release validation, drift checks, README, and trust docs to ship and validate `.agents/skills/**`.
**Status**: completed

## [2026-05-19] Topic: Codex subagents in Zed and CLI
**Files**: `.codex/config.toml`, `.codex/agents/*.toml`, `.agents/skills/codex-subagent-orchestration/SKILL.md`, `docs/CODEX_SUBAGENTS_AUDIT.md`, `scripts/validate-codex-agents.js`, `AGENTS.md`, `README.md`, `setup.sh`, `setup.bat`, `scripts/sync-template.sh`, `scripts/test-template.sh`, `scripts/validate-template.sh`, `scripts/check-drift.sh`
**Findings**: `codex-cli 0.125.0` reports `multi_agent stable true`. Real `codex exec --json` in the trusted repo successfully called `spawn_agent` and `wait`; a custom repo-scoped `.codex/agents/probe-reader.toml` returned a marker that existed only in the TOML file. A temp project outside trusted repo returned `unknown agent_type`, so project trust/location matters. Zed runs managed `codex-acp.exe` from `AppData/Local/Zed/external_agents/registry/codex-acp/v_0.10.0...`; child-thread UI visibility may lag CLI/app, but native Codex config is the right project surface.
**Decisions**: Add seven Spark-backed template Codex subagents in `.codex/agents`: `pr_explorer`, `reviewer`, `security_reviewer`, `tester`, `docs_researcher`, `design_reviewer`, `implementer`. Keep read-only fan-out as default, limit recursion with `[agents] max_depth = 1`, and use `implementer` only for isolated non-overlapping files. Add `scripts/validate-codex-agents.js` and wire `.codex/agents/*.toml` into setup/sync/test/validate.
**Status**: completed

## [2026-04-21] Topic: production-ready recovery handoff
**Files**: `tasks/current.md`, `tasks/template-production-ready-plan.md`, `tasks/debug-recovery-log.md`, `tasks/lessons.md`, `scripts/context-restore.sh`, `scripts/session-metrics.sh`, `.claude/hooks/pre-compact.sh`, `.claude/hooks/session-start.sh`
**Findings**: `tasks/current.md` had become a stale March plan plus session-end dumps; the real roadmap lives in `tasks/template-production-ready-plan.md`; the real recovery state for today's Zed/Codex failure lives in `tasks/debug-recovery-log.md`; raw `head -30 tasks/current.md` in `scripts/context-restore.sh` hid the important state as the handoff grew; research-cache counters also matched the example heading in `tasks/.research-cache.md` and overstated the number of entries; the Phase 3 validation trio (`validate-template`, `check-drift`, `test-hooks`) now passes after fixing version drift, validator self-matches, non-portable `grep -P`, and section-naive tool-registry parsing; bootstrap/source-of-truth cleanup then exposed that `setup.*` was copying directory trees instead of tracked payload, which leaked maintainer-local `.claude/settings.local.json`, copied built `mcp-servers/context-router/node_modules`, and shipped live recovery/task history instead of clean starter state; after broadening payload discovery for working-tree smoke, a second leak appeared: top-level `brain/*` and `tasks/*` allowlists were still too coarse and copied maintainer research markdown plus `tasks/audit/*.jsonl`; on real downstream repos, a new Windows portability issue appeared: when Git Bash is launched from PowerShell, some scripts resolve `SCRIPT_DIR` as `/C/...`, fail to source `scripts/lib/platform.sh`, and lose `_node`; the current template now compensates for that and can run sync against a target project via `--project-dir`; `docs/MIGRATION_MATRIX.md` records real dry-run outcomes for `3.4.0`, `2.7.0`, and `unknown` downstream projects; `PROJECT_SPEC.md` now has a real generator path instead of a stale placeholder; `_reference/tool-registry.md` is now populated by `scripts/scan-project.sh` instead of being a static promise; the GitHub workflow previously referenced a non-existent nested repo path and is now rewritten around the actual root plus Linux/Windows bootstrap smoke; a final trust-hardening pass found one more leak vector: `setup.*` still unioned `git ls-files` with recursive working-tree scans, so brand-new untracked files under `docs/` or `scripts/` could still ship; this is now closed, guarded by new product-boundary/safe-defaults/environment docs, and covered by local plus CI untracked-sentinel bootstrap smoke; `scan-project.sh --report` was also validated on a real downstream repo (`YokhanCallService`) and stayed concise.
**Decisions**: Rewrite `tasks/current.md` into a short handoff that links to the detailed roadmap; keep attempt-level debugging in `tasks/debug-recovery-log.md`; keep project `.codex/config.toml` limited to project-specific settings only; count real research-cache entries by dated heading pattern, not by generic `## [` prefix; treat the validation layer as green baseline and move next to source-of-truth/bootstrap cleanup; copy project payload from tracked files only via `git ls-files`; layer starter overrides from `templates/project-starter/` for `tasks/current.md`, `tasks/.research-cache.md`, `tasks/lessons.md`, empty `brain/01-daily`, and empty knowledge/audit directories; extend `sync-template.sh` coverage to `.codex/`, `.claude/docs/`, MCP package-lock/tsconfig, root bootstrap docs, and project spec files; add `scripts/task-brief.sh` plus `--brief/--json` output so session-start and compaction can consume a compact handoff instead of dumping raw markdown; keep a narrow banlist for mixed scaffold/history paths such as `brain/03-knowledge/{research,audits}` and `tasks/audit`; normalize uppercase drive-letter POSIX paths before sourcing shared shell helpers; use template-owned `sync-template.sh --project-dir` as the supported escape hatch when legacy local sync scripts are broken; use `scripts/downstream-census.sh` plus `docs/MIGRATION_MATRIX.md` as the repeatable migration-proof surface; use `scripts/generate-project-spec.sh` and `scripts/scan-project.sh` as the baseline automation path for spec/registry freshness; use `.github/workflows/validate-template.yml` plus `docs/RELEASE_CHECKLIST.md` as the release contract; treat `README.md` and `SETUP_GUIDE.md` as template-owned bootstrap docs after `/setup-project`; treat trust checks as tracked-state checks, not local-untracked-state checks.
**Status**: completed

## [2026-04-21] Topic: AgentOS compatibility contract
**Files**: `scripts/generate-project-spec.sh`, `PROJECT_SPEC.md`, `../AgentOS/src-tauri/src/commands/ops.rs`, `../AgentOS/src-tauri/src/scanner.rs`, `../AgentOS/src-tauri/src/commands/claude_runner.rs`, `../AgentOS/setup.sh`, `../AgentOS/scripts/sync-template.sh`, `../AgentOS/CLAUDE.md`
**Findings**: `AgentOS` is not just a sibling repo; it is the orchestrator that creates and updates projects from this template. Before this pass it still carried stale embedded copies of template bootstrap/update scripts, so create/deploy flows could drift away from the actual template contract. Its scanner read `phase` only from an older `PROJECT_SPEC.md` spelling, which left current template projects without a phase even though the live milestone already existed in `tasks/current.md`. On this Windows machine `bash` is also not in `PATH`, so any `silent_cmd("bash")` call is a latent runtime failure even when Git Bash is installed. The template-side `PROJECT_SPEC` generator now emits `- Phase:` from the current handoff milestone and no longer uses the unsafe `grep -c ... || echo 0` pattern in zero-match branches.
**Decisions**: Treat `agent-project-template` as the only source of truth for managed-project bootstrap and sync logic. Let `AgentOS` orchestrate by calling that contract, not by re-embedding its own template implementation. In `AgentOS`, resolve the sibling `documents_dir/agent-project-template` for create/deploy, fall back from `PROJECT_SPEC.md` to `tasks/current.md` milestone during scans, and resolve Git Bash explicitly on Windows. Keep root-level `AgentOS/setup.sh` and `AgentOS/scripts/sync-template.sh` as compatibility shims that delegate to the canonical template repo instead of preserving stale forked logic.
**Status**: completed
