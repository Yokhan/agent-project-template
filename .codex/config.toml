# Codex CLI configuration for agent-project-template
# Docs: https://developers.openai.com/codex/config-basic
# All other settings (model, effort, approval, sandbox) come from Zed UI / ~/.codex/config.toml

[features]
codex_hooks = true
