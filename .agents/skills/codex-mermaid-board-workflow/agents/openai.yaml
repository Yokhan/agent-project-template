interface:
  display_name: "Codex Mermaid Board Workflow"
  short_description: "Mermaid control boards and project maps"
  default_prompt: "Use $codex-mermaid-board-workflow to create or maintain a Mermaid control board with .mmd as the source of truth."
policy:
  allow_implicit_invocation: true