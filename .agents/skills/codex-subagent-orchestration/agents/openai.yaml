interface:
  display_name: "Codex Subagent Orchestration"
  short_description: "Fan out work to Codex subagents"
  default_prompt: "Use $codex-subagent-orchestration to split parallelizable work across `.codex/agents` workers and consolidate results safely."
policy:
  allow_implicit_invocation: true
