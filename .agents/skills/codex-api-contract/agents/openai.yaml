interface:
  display_name: "Codex API Contract"
  short_description: "Validate API contracts"
  default_prompt: "Use $codex-api-contract to verify this API change against its contract."
policy:
  allow_implicit_invocation: true
