interface:
  display_name: "Codex Pipeline Workflow"
  short_description: "Run gated agent workflows"
  default_prompt: "Use $codex-pipeline-workflow to execute this task through the right gated workflow."
policy:
  allow_implicit_invocation: true
