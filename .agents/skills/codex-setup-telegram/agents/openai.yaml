interface:
  display_name: "Codex Setup Telegram"
  short_description: "Set up Telegram integration safely"
  default_prompt: "Use $codex-setup-telegram for Telegram bot, remote-control, or chatops setup in Codex projects."
policy:
  allow_implicit_invocation: true
