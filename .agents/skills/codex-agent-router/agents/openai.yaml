interface:
  display_name: "Codex Agent Router"
  short_description: "Route work to Codex skills"
  default_prompt: "Use $codex-agent-router to route Codex work to repository skills, shared rules, plans, memory, and verification gates."
policy:
  allow_implicit_invocation: true
